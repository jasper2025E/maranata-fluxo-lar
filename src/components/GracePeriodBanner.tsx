// GracePeriodBanner - Not used in single-tenant system
// Keeping as empty component for backwards compatibility

export function GracePeriodBanner() {
  // Single-tenant system has no subscription management
  return null;
}
